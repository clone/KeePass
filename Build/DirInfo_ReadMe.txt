/Build

This directory contains all binary builds of KeePass applications and
libraries.

All subdirectories can be deleted, they will be recreated when building
the binaries. Files in the root path (/Build) must not be deleted!
